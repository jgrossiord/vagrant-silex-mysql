{% extends 'layout.twig' %}
{% block body %}
<h1>Users</h1>
<ul>
    {% for user in users %}
        <li><a href=/users/{{ user.id }}>{{ user.firstname|e }} {{ user.lastname|e }}</a></li>
    {% endfor %}
</ul>
{% endblock %}